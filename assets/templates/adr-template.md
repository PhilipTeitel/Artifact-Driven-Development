# ADR-NNN: {Short title in imperative or noun phrase}

**Status:** Proposed | Accepted | Superseded (if Superseded, link: `ADR-MMM-...`)
**Date:** {YYYY-MM-DD}

---

## Context

{What problem or force triggered this decision? What constraints apply (platform, security, performance, team, prior commitments)?}

---

## Decision

{State the decision in plain language — what we will do and what stack or API boundary we commit to.}

---

## Consequences

**Positive**

- ...

**Negative / costs**

- ...

---

## Alternatives considered

| Alternative | Why not chosen |
|---------------|----------------|
| ... | ... |

---

## Explicit non-decisions

{What this ADR does **not** decide — to prevent implementers from expanding scope or substituting backends. Use bullets.}

- This ADR does not require ...
- Out of scope: ...

---

## Links

- Requirements: `{path/to/requirements.md}` (or "see configured design doc Requirements section")
- Related design doc section: `{e.g. Key Design Decisions — ...}`
- Related stories: `{STORY-ID}` (optional)
- Supersedes / superseded by: `{ADR-xxx}` (if any)

---

## File naming

In the **target project repo**, save using the configured decisions directory and ADR naming pattern. Default:

`docs/decisions/ADR-NNN-short-slug.md`

Use sequential three-digit `NNN` unless the profile overrides the pattern. Do not renumber existing ADRs; append the next number.
