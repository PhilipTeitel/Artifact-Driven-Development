<!-- Superseded.

Per-path comparison rules and oracle sources live on the requirements file
(`REQ-NNN` section 4b and Constraints). Story tests live in the user-story
Test Plan as `parity` rows.

Do not write `docs/modernization/test-plans/`. See `requirements-template.md`
and `user-story-template.md`.
-->
