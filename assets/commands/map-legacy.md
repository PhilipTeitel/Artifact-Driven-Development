# map-legacy

**Replaced by** [`/inventory-paths`](inventory-paths.md).

This command is a compatibility alias. Do not write `docs/modernization/legacy-map.md`.

**Command-agent binding:** This command is role-bound to `agents.archaeologist`. Before executing any step, load the configured Archaeologist agent definition, then execute `/inventory-paths` with the same arguments and follow `inventory-paths.md` as the source of truth.
