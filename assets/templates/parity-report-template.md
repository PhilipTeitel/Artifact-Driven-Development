<!-- Superseded.

Parity evidence is recorded in the story QA matrix for criteria covered by
`parity` test rows. `/qa-story` is the verifier. Do not write a separate
parity report under `docs/modernization/parity/`.
-->
