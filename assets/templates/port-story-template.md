<!-- Superseded.

Port delivery uses the ordinary user-story template. Recovered legacy behavior
reaches the story through REQ-NNN section 4b (Legacy provenance). `/plan-story`
copies those rows into story section 1b, adds slice prerequisites when needed,
and plans `parity` test rows in section 8a.

Do not fill this file. See `user-story-template.md`.
-->
