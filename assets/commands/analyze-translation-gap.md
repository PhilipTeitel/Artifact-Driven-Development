# analyze-translation-gap

**Replaced by** [`/analyze-impedance`](analyze-impedance.md).

This command is a compatibility alias. Do not write `docs/modernization/translation-gaps.md`. Do not fill seeded C / Java / .NET matrices with "not applicable" rows.

**Command-agent binding:** This command is role-bound to `agents.migrationStrategist`. Before executing any step, load the configured Migration Strategist agent definition, then execute `/analyze-impedance` with the same arguments and follow `analyze-impedance.md` as the source of truth.
